name: Deploy Frontend to AWS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install npm packages
        run: npm install

      - name: Fix vite permissions
        run: chmod +x node_modules/.bin/vite || true

      - name: Build frontend
        run: npm run build

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-2

      - name: Generate version
        id: version
        run: |
          VERSION="v$(date +%Y%m%d%H%M%S)"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Upload versioned build
        run: |
          aws s3 sync dist/ s3://exam-frontend-production/${{ steps.version.outputs.version }}/

      - name: Clear latest folder
        run: |
          aws s3 rm s3://exam-frontend-production/latest/ --recursive

      - name: Upload latest build
        run: |
          aws s3 sync dist/ s3://exam-frontend-production/latest/

      - name: Update releases.json
        run: |
          echo "{ \"latest\": \"${{ steps.version.outputs.version }}\" }" > releases.json
          aws s3 cp releases.json s3://exam-frontend-production/releases.json \
            --content-type application/json

      - name: Invalidate CloudFront Cache
        env:
          CLOUDFRONT_DIST: ${{ secrets.CLOUDFRONT_DIST }}
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "$CLOUDFRONT_DIST" \
            --paths "/*"
