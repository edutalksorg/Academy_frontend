# College Placement Frontend

Vite + React + Tailwind frontend scaffold for the College Placement & Student Management platform.

Quick start

1. Install dependencies:

```powershell
cd frontend
npm install
```

2. Run dev server (proxy to backend at `/api` configured in `vite.config.js`):

```powershell
npm run dev
```

3. Default pages: `/login`, `/register`.
